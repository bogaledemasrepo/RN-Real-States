export interface Property {
  id: string;
  name: string;
  location: string;
  category: string;
  price: string;
  rating: string;
  imageUrl: string;
}

export interface User {
  id: string;
  name: string;
  email: string;
  avatar: string;
}
